[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "vero_ssz_grandine_py"
version = "0.1.0"
description = "Python bindings for Grandine SSZ"
requires-python = ">=3.12"
